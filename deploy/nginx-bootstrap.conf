server {
    listen 80;
    server_name 101.200.191.177;
    access_log off;
    location /.well-known/acme-challenge/ { root /var/www/acme; }
    location / {
        default_type text/plain;
        return 503 "Canvas HTTPS setup in progress. Please retry shortly.\n";
    }
}

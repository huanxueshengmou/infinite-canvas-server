# 协作服务验收记录

## 自动检查

- 前端 TypeScript 检查、Vite 生产构建通过；构建仍提示原项目已有的大包和重复动态导入问题。
- 服务端安全与恢复测试通过，包含来源/CSRF、会话失效、分享口令/过期/撤销、只读越权、隐私隔离、公共协议白名单、操作幂等、并发冲突、事务回滚、超限拒绝、SSRF/DNS 检查、加密文件、备份恢复、防覆盖、挂载失效、并发背压、未知数据库版本拒绝。
- 新增服务端依赖 `npm audit --omit=dev`：0 项漏洞。
- 实际 HTTPS 出站请求返回 HTTP 200，验证 DNS 固定与 TLS 连接参数。
- 当前服务端共 **55 项测试**在本机和服务器 Linux 容器通过，无跳过项；隔离容器显式设置 `NODE_ENV=test`。本机约 17.73 秒，服务器约 29.02 秒。
- 分组覆盖原子创建 / 整体移动 / 解散 / 删除、撤销重做、过时版本、跨画布归属、嵌套与隐私成员拒绝，以及历史不能让协作者新加入的成员悬空。白板覆盖笔迹与尺寸、缩小保留笔迹、严格内容白名单、只读拒绝及无输入 / 输出连线。图片替换只更新当前节点，旧文件保留可下载并可撤销。
- 文件导入覆盖后缀分类、缺少或伪报 MIME、未知后缀仅下载、媒体节点伪造拒绝、中文文件名与下载头清理、跨画布越权和只读上传拒绝；上传复用现有传输队列时，画布编辑和健康检查仍可完成。Markdown 覆盖同步、文本连线输入、撤销恢复和只读保护。
- 新增覆盖批量拖动合并历史、节点与连线原子恢复、历史幂等、越权及协作者冲突、较新私有配置与结果保留；同时验证多种 HTTP 方法与请求正文、自定义认证、模板秘密清空、白名单 / 黑名单独立保存及 DNS 安全边界。
- multipart 覆盖真实加密附件、重复字段、仅普通字段、空表单、提前拒绝与中途取消，核对完整边界、精确长度和临时密文清理。数据库 v1 / v2 升级 v3 均覆盖 WAL 数据与迁移前备份。
- 协议保护覆盖缺少、过时或未知版本的节点、文件上传、历史、隐私保存 / 执行 / 发布、模板和域名策略写入，拒绝时数据和版本保持不变且不调用供应商；旧 WebSocket 提示刷新并关闭。当前协议版本为 5，数据库保持版本 3。

## 图片编辑、分组与白板浏览器验证（协议 5）

- 两个独立 Chrome 上下文完成图片画笔、箭头、文字、框选裁剪和局部撤销重做。导出的 300 × 200 PNG 核对背景、笔迹像素和文字 / 箭头区域；系统剪贴板复制后以原生 Ctrl+V 粘贴，解码像素摘要与预览一致，原节点不变。
- 覆盖图片只更新当前节点，引用旧文件的其他节点保持原图，右键下载原文件字节与中文名称正确；撤销重做可恢复文件引用。协作者换图后覆盖明确拒绝，结果弹窗保留并可复制。
- 分组验证整体拖动与一次撤销、重做、拖出去再回到起点、成员单独移动、拖入其他组、移出 / 解散及恢复，复制组后成员 ID 正确重映射。组内局部框选不会选中整个组，空白处双击显示节点类型，组内连线可点击选中。
- 白板验证手写、多行文字、协作者同步、自由调整大小及撤销重做；缩到 80 × 60 再放大仍保留全部笔迹，白板和组均无端口。只读成员不能新增、调整大小或提交笔迹；刷新后笔迹与分组关系保留。
- 390 px 和 800 px 窗口中的工具栏可完整操作，手机图片编辑弹窗无横向溢出，明暗主题截图检查通过，页面运行时异常为 0。原文件导入浏览器脚本回归通过，覆盖 Markdown、拖入、系统图片粘贴、节点复制、图片 / 视频 / 文件真实下载、只读保护和手机类型选择。

## 20 人协作编辑基线（协议 3）

机器为 2 核、约 1.8 GiB 内存；生成器与服务使用独立进程，在 768 MiB 限额的 Docker 测试容器内运行，生产服务同时保持在线。20 个独立账户保持连接，各执行 100 次编辑，并发送各自鼠标位置，共 2,000 次写入。协议 3 版本测试约 27.94 秒：

| 指标 | 结果 |
| --- | --- |
| 确认延迟 P50 | 79.00 ms |
| 确认延迟 P95 | 134.84 ms |
| 确认延迟 P99 | 173.99 ms |
| 最慢确认 | 226.90 ms |
| 服务端峰值 RSS | 151.6 MiB |
| 事件循环延迟 P95 | 13.00 ms |
| 事件丢失 / 最终分歧 | 0，全部客户端一致 |
| 具名鼠标 | 20 个，所有客户端用户名和最终坐标一致 |
| 广播中的私有哨兵 | 未发现 |

这是服务器内真实网络连接的文本及位置编辑压力测试，不含公网 RTT、20 个同时执行的 AI 请求或大文件压力。API / 文件处理并发为 2；默认 50 连接是准入配置，不代表已经完成 50 人负载验收。

## 500 MiB multipart 发送基线（协议 3）

服务器的独立 768 MiB 容器使用本地加密附件，经过完整认证、multipart 编码和流式解析，附件原始大小为 524,288,000 字节，请求总长为 524,288,418 字节。编码与解析约 8.01 秒，同进程峰值 RSS 165.8 MiB；解析后文件 SHA-256、重复字段与声明长度均一致，临时密文已清理。

该历史检查覆盖当时新增的编码与读取路径；编码器与接收解析器处于同一进程，未连接真实供应商，也不代表公网或云盘吞吐。

## 500 MiB 文件上传与下载基线（协议 2）

在同一台服务器的独立 768 MiB 测试容器中，以本地临时数据库和指定团队云盘的独立测试目录验证；生产数据库和已有文件不参与测试。压力生成器与服务为独立进程，生成器使用遵循网络背压的字节流，分别记录内存：

| 项目 | 结果 |
| --- | --- |
| 单文件 524,288,001 字节 | HTTP 413，未留下截断文件 |
| 两路 524,288,000 字节上传 | 均为 HTTP 200，约 25.34 / 25.55 秒 |
| 两路 500 MiB 下载 | SHA-256 与原始内容一致，约 28.38 / 28.43 秒 |
| 完整认证后的首字节等待 | 约 14.96 / 15.11 秒 |
| 服务端峰值 RSS | 150.4 MiB |
| 压力生成器峰值 RSS | 139.6 MiB |
| 文件传输期间健康检查 P95 | 229.47 ms |
| 临时密文清理 | 处理槽释放后无下载缓存残留 |

本机独立测试也通过相同文件边界与哈希校验。以上速度来自服务器内网路径和当时云盘挂载状态，不代表公网带宽或后续云盘速度，也不将挂载报告的容量视为用户套餐容量的验证。20 人编辑测试与大文件测试分别进行，未声称 20 人同时生成视频或上传 500 MiB 文件。

正式入口另行通过受信任 HTTPS / Nginx 验证：精确 500 MiB 上传约 19.32 秒，下载约 15.28 秒且 SHA-256 一致；下载完整认证后的首字节约 8.06 秒，超出 1 字节返回 HTTP 413。连接在服务器内回到正式网关，保持证书验证，不代表公网上传速度。网关容器已重建，容器内外配置哈希一致；检查未发现无引用密文或下载缓存残留。

## 本轮浏览器验收

- 三个独立 Chrome 上下文验证编辑者与只读成员的多文件拖入、文件选择、原生图片剪贴板粘贴、视频 / 附件粘贴事件，以及所选公共节点 Ctrl+C / Ctrl+V、重复粘贴与撤销重做；复制媒体节点不重复上传文件。输入框保留文字粘贴；批次上传后再发布节点，避免新预览与该批上传争用传输通道。
- 真实 PNG 可解码，真实 WebM 可播放；图片 / 视频 / 附件右键菜单的整行点击均下载成功，逐字节核对内容，保留中文与特殊字符文件名。菜单使用 `data-canvas-no-zoom`，避免点击被画布平移截获。未知后缀、损坏媒体均保留下载入口。
- 全选 Markdown 文字后点击画布空白处、节点标题或 Ctrl 多选，会退出输入焦点并清除残留文字选区；随后原生图片粘贴、节点复制粘贴、视频 / 附件粘贴与右键下载均通过，输入框文字粘贴及协作者同步仍正常。Chrome 会重编码系统剪贴板 PNG，验收分别核对图片像素和浏览器实际交给画布的文件字节。
- 双击空白处选择节点类型或已有模板，节点创建在对应画布坐标；平移和缩放后的拖入落点按屏幕像素核对。Markdown 编辑、导入、渲染、刷新和协作者同步通过，HTML / 危险链接不执行，外部图片不自动请求；文件大小或 JSON 转义后超出既有同步上限时保留为附件。
- 文件与 Markdown 验收共创建 22 个隔离节点；页面运行时异常和外部 Markdown 请求均为 0。390 px 手机选择框在动画结束后核对布局，节点类型、模板及滚动区域均可操作。文件 / 视频的系统剪贴板暴露方式仍取决于浏览器，真实 macOS 手感和不同视频编码保留人工验收。
- 两个独立 Chrome 上下文验证拖到节点本体连线、右键断线、Ctrl / Meta 撤销重做、框选与点选多选、跨两次同步的拖动合并、批量删除并恢复关联线。输入框保留原生快捷键，协作者后续修改保留。
- 同一私有窗口连续发送 XML、URL 编码表单和 multipart 请求，核对 PATCH、原样认证、重复字段与实际请求内容；模板不保存密钥或秘密头值。固定发送按钮的无障碍名称，避免加载图标改变其名称。
- 管理窗口反复切换两种域名模式，核对两份名单独立保留；私有输出拖到节点时可选择输入类型。只读账户的新增入口和文字编辑受限，快捷键与右键操作均不写入数据，也看不到他人私有入口。
- 旧工作流回归覆盖鼠标与缩放、自定义计算、模板保存与实例化、图片预览与明确发布、视频任务提交与单独查询，以及管理界面调整上传上限。
- 390 px 手机画布、模板和展开后的请求窗口可操作且无横向溢出；检查自定义头换行、格式选择、XML 正文和保存按钮。页面运行时异常为 0。Meta 键路径使用 Chrome 自动化验证，真实 macOS 的快捷键手感仍在人工待测清单中。
- 上游响应采用确定性模拟数据，未使用真实 API 密钥或产生供应商费用。真实供应商参数、模型名、生成质量、耗时及媒体域名仍需业务验收。

## 本轮正式部署（协议 5）

- 正式 HTTPS 入口已运行协议 5，数据库保持 v3。最终镜像在隔离 Linux 容器通过全部 55 项测试及启动检查，实际运行容器内 53 个应用 / 静态文件的 SHA-256 与构建输入一致。
- 切换前验证在线备份；暂停应用后再生成最终本地 SQLite 快照及云盘加密备份，并恢复到独立文件逐表核对。切换前后 13 张表和主密钥完全一致，保留旧镜像、源码、配置及备份。
- 正式页面用两个临时账户通过图片画笔、箭头、文字、裁剪、PNG 像素核对、系统剪贴板粘贴、覆盖与撤销重做、原图下载及并发换图保护。分组整体 / 单独拖动、组内框选与连线、成员移入移出、复制、解散与恢复、白板书写 / 文字 / 缩放、只读保护和刷新均通过。
- 正式页面检查 390 px / 800 px 窗口与明暗主题，页面运行时异常为 0。验收按弹窗动画完成后的坐标落笔和取样；未调用真实 AI 供应商或消耗生成额度。
- 本轮测试账户、画布与文件已按各自清单清理，清理前备份并独立恢复，测试文件密文归档后核对哈希，清理事务内确认所有非测试记录不变。清理后再备份并恢复，原有 **6 个账户、3 个画布、32 个节点、31 个文件、94 条操作历史**及其余表记录与切换前一致，数据库完整性与外键检查通过。
- 本轮没有重跑此前的 20 人及 500 MiB 压测，相关数据仍以标注的历史协议基线为准。

## 此前正式部署（协议 4）

- 正式 HTTPS 入口已运行协议 4，数据库保持 v3。最终镜像先在独立容器验证启动、健康检查、静态页面和协议；37 个后端文件与通过 47 项测试的镜像一致，最后的输入焦点修复只影响前端。
- 切换前完成在线备份与独立恢复；暂停应用后再生成最终 SQLite 快照和云盘加密备份，恢复核对通过后切换。切换前后 13 张表的记录摘要及主密钥一致；旧镜像、源码与快照保留。
- 正式页面通过多文件拖入、Markdown 编辑 / 导入 / 同步、双击节点类型与模板、原生图片粘贴、公共节点复制、撤销重做、未知后缀仅下载、视频播放、只读保护、旧协议拒绝、刷新和手机布局验收。图片、视频和附件右键下载实际完成，原始字节与中文文件名正确。
- 最终版本另用两个临时账户验证文字全选后点击空白处、节点标题及 Ctrl 多选，再进行图片 / 视频 / 附件粘贴和节点复制；输入框文字粘贴与协作者同步保留，三种右键下载与只读成员下载通过，浏览器运行时异常为 0。
- 本轮临时验收账户、画布与文件已按清单清理，测试密文先归档并核对哈希；清理事务前后核对所有非测试记录一致。清理后再次备份并独立恢复，原有 **6 个账户、3 个画布、10 个节点、7 个文件、40 条操作历史**与切换前完全一致，完整性与外键检查通过。
- 实际运行容器内 51 个应用 / 静态文件的 SHA-256 与构建输入一致。应用健康、原有云盘挂载和证书续期 timer 正常；本轮未重新执行前述协议 3 的 20 人及 500 MiB 压测。

## 此前正式部署（协议 3）

- 已在正式 HTTPS 入口部署协议 3，并将数据库从 v2 升级为 v3。升级前完成在线备份验证；暂停应用后再次生成最终 SQLite 快照和云盘加密备份，恢复到独立文件并逐表核对，再切换应用。
- 迁移后及线上验收清理后，两次完整核对原有记录摘要一致：6 个账户、3 个画布、7 个节点、5 个文件，以及既有会话、邀请、成员、设置与审计记录均保留。主密钥指纹一致，完整性与外键检查通过，自动迁移备份存在；旧镜像、源码、配置与迁移前快照保留在服务器。
- 两个临时普通账户通过受信任 HTTPS 验证具名鼠标、拖到节点本体连线、右键断线、撤销重做、框选与批量移动、批量删除并恢复节点和线、自定义计算、刷新恢复，以及私有内容与模板隔离。新版请求配置可保存，模板中的秘密头值被清空，旧协议 2 的节点与域名策略写入被拒绝；未调用真实供应商。
- 临时账户、画布、模板和对应加密操作历史已按本次记录的 ID 清理。清理后创建新版云盘加密备份并恢复到新的本地文件，数据库版本为 3，逐表记录一致，完整性为 `ok`，未覆盖运行中的数据库。
- 运行容器内 49 个应用与静态资源文件的哈希与已验证的构建输入一致；源码与提交内容已核对。应用健康、云盘挂载及证书续期 timer 正常；390 px 正式页面无横向溢出，页面运行时异常为 0。

## 此前正式部署记录（协议 2）

- 协作协议版本 2 已部署。版本 1 主库升级前完成云盘加密备份和本地独立快照；迁移后逐表校验既有记录一致，主库完整性与外键检查通过，自动迁移备份存在。
- 正式 HTTPS 上用两个临时普通账户验证具名鼠标、拖线、刷新恢复、自定义计算、个人模板隔离、隐私拒绝访问和旧协议写入拒绝；管理员上传限额读取 / 保存与退出登录通过。临时账户、画布和文件已清理，既有账户与节点再次逐项校验一致。
- 清理后从实际云盘加密备份恢复到新的本地文件，数据库版本为 2，完整性为 `ok`，既有记录和 500 MiB 上传配置均正确；没有覆盖运行中的主库。云盘挂载、应用健康检查和证书续期 timer 正常。
- 两个独立 Chrome 上下文验证用户名鼠标、缩放对齐、拖线、重接和刷新后连线恢复、自定义节点计算、模板保存与再次实例化、生图响应预览和明确发布、视频任务提交与单独查询、管理界面调整上传上限。390 px 手机画布与模板窗口可操作且无横向页面溢出，页面运行时异常为 0。
- 生图和视频验收使用确定性模拟响应，证明请求参数、任务 ID、预览和分享路径；没有使用用户的真实 API 密钥或产生供应商生成费用。真实模型名、生成质量、任务耗时以及供应商 / 媒体域名仍需要用户填入自己的配置进行业务验收。
- 隔离浏览器完成登录页自助注册、两次密码一致性校验、自动登录、刷新后会话恢复、创建自己的画布、退出后重新登录及邀请注册；新账号看不到既有画布和服务管理，手机注册页无横向溢出，页面异常为 0。
- 两个独立浏览器上下文完成创建画布、公开文本同步、创建/保存隐私节点、生成只读邀请、新用户注册加入、只读输入校验；接收者看不到私有标题、备注及密钥。撤销分享后接收者在线连接立即失效。未出现页面运行时异常。
- `http://101.200.191.177` 跳转到受信任 HTTPS，`/health` 返回 `{"status":"ok"}`。
- 线上 Chrome 独立实例完成真实管理员登录、画布 WebSocket 连接、云盘备份状态检查和退出登录；手机尺寸登录页无横向溢出，页面运行时异常为 0。
- 指定团队云盘实际完成加密文件上传、下载核对、加密数据库备份；真实云盘备份恢复到新的本地文件，SQLite 完整性检查为 `ok`，账户/画布数据可读取。
- IP 证书续期 dry-run 成功，续期 timer 已启用。
- 旧白板容器和旧 WebDAV 服务已退出；`mountapp` 及 `/root/PDSDrive` 挂载保持正常。

仍建议按项目待测试清单，用真实团队账户、常用 API 服务商、手机和实际图片大小进行业务验收。不会把上述文本同步测试解释为所有媒体与公网条件下的容量保证。
